[build-system]
requires = ["maturin>=1.7"]
build-backend = "maturin"

[project]
name = "agent-orchestrator-rust"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "_agent_orchestrator_rust"
