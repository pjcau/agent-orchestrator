name: Nightly Research Scout

on:
  schedule:
    # Run every night at 02:00 UTC
    - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      lookback_days:
        description: 'Number of days to look back for starred repos'
        required: false
        default: '30'

permissions:
  contents: write
  pull-requests: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  research:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'

      - name: Install dependencies
        # The harness install pulls PyYAML and other deps that
        # scripts/run_research_scout.py needs transitively via
        # agent_orchestrator.core. aiohttp is for fetch_github_stars.py.
        run: |
          pip install -e .
          pip install aiohttp

      - name: Fetch GitHub starred repos
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_USERNAME: ${{ vars.GH_USERNAME }}
          LOOKBACK_DAYS: ${{ github.event.inputs.lookback_days || '30' }}
        run: python scripts/fetch_github_stars.py

      - name: Analyze one repo and propose improvements
        id: scout
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          LOOKBACK_DAYS: ${{ github.event.inputs.lookback_days || '30' }}
        run: |
          echo "::add-mask::$OPENROUTER_API_KEY"
          python scripts/run_research_scout.py
          if [ -f .claude/research-scout-findings.md ]; then
            echo "has_findings=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_findings=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Usage report
        if: always()
        run: |
          echo "## Research Scout Usage Report" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          if [ -f .claude/research-scout-usage.json ]; then
            echo "| Metric | Value |" >> "$GITHUB_STEP_SUMMARY"
            echo "|--------|-------|" >> "$GITHUB_STEP_SUMMARY"
            python -c "
          import json
          u = json.load(open('.claude/research-scout-usage.json'))
          print(f'| GitHub API calls | {u[\"github_api_calls\"]} |')
          print(f'| Characters fetched | {u[\"chars_fetched\"]:,} |')
          print(f'| LLM model | {u[\"llm_model\"] or \"none\"} |')
          print(f'| LLM input tokens | {u[\"llm_input_tokens\"]:,} |')
          print(f'| LLM output tokens | {u[\"llm_output_tokens\"]:,} |')
          print(f'| LLM total tokens | {u[\"llm_total_tokens\"]:,} |')
          print(f'| **LLM cost** | **\${u[\"llm_cost_usd\"]:.4f}** |')
          " >> "$GITHUB_STEP_SUMMARY"
          else
            echo "No usage data available." >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Outcomes report (last 14 days)
        if: always()
        run: |
          # Renders why each of the last 14 nightly runs did or didn't
          # produce a PR. Lives in the workflow step summary so the
          # operator can spot patterns (e.g. "4 days in a row of 429")
          # without having to read every state-update commit.
          echo "" >> "$GITHUB_STEP_SUMMARY"
          python scripts/explain_research_scout_history.py --days 14 >> "$GITHUB_STEP_SUMMARY" || true

      - name: Create PR with improvements
        if: steps.scout.outputs.has_findings == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          BRANCH="research-scout/$(date +%Y-%m-%d-%H%M)"
          git config user.name "research-scout[bot]"
          git config user.email "research-scout[bot]@users.noreply.github.com"
          git checkout -b "$BRANCH"
          # Only commit state files — findings.md is used as PR body, not committed
          git add .claude/research-scout-state.json .claude/bookmarks.json
          git diff --cached --quiet && exit 0
          git commit -m "research-scout: improvement proposal from $(date +%Y-%m-%d)"
          git push -u origin "$BRANCH"
          gh label create "research-scout" --description "Automated research scout improvements" --color "0E8A16" 2>/dev/null || true
          gh pr create \
            --title "research-scout: improvement proposal $(date +%Y-%m-%d)" \
            --body-file .claude/research-scout-findings.md \
            --label "research-scout"

      - name: Update state on main
        run: |
          git config user.name "research-scout[bot]"
          git config user.email "research-scout[bot]@users.noreply.github.com"
          git checkout main
          git add .claude/research-scout-state.json .claude/bookmarks.json
          git diff --cached --quiet || { git commit -m "research-scout: update state" && git push; }
