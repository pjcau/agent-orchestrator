services:
  # PostgreSQL for checkpointing
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: orchestrator
      POSTGRES_PASSWORD: orchestrator
      POSTGRES_DB: orchestrator
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U orchestrator"]
      interval: 5s
      timeout: 3s
      retries: 5

  # Python development environment
  app:
    build:
      context: .
      dockerfile: docker/app/Dockerfile
    volumes:
      - .:/workspace
    working_dir: /workspace
    environment:
      - PYTHONPATH=/workspace/src
      - DATABASE_URL=postgresql://orchestrator:orchestrator@postgres:5432/orchestrator
    depends_on:
      postgres:
        condition: service_healthy
    command: sleep infinity

  # Test runner
  test:
    build:
      context: .
      dockerfile: docker/app/Dockerfile
    volumes:
      - .:/workspace:ro
      - test-results:/results
      - pytest-cache:/tmp/pytest-cache
    working_dir: /workspace
    environment:
      - PYTHONPATH=/workspace/src
    command: pytest --tb=short --junitxml=/results/report.xml -o cache_dir=/tmp/pytest-cache

  # Linter
  lint:
    build:
      context: .
      dockerfile: docker/app/Dockerfile
    volumes:
      - .:/workspace:ro
      - ruff-cache:/tmp/ruff-cache
    working_dir: /workspace
    environment:
      - RUFF_CACHE_DIR=/tmp/ruff-cache
    command: ruff check src/ tests/

  # Format checker
  format:
    build:
      context: .
      dockerfile: docker/app/Dockerfile
    volumes:
      - .:/workspace:ro
      - ruff-cache:/tmp/ruff-cache
    working_dir: /workspace
    environment:
      - RUFF_CACHE_DIR=/tmp/ruff-cache
    command: ruff format --check src/ tests/

volumes:
  pgdata:
  test-results:
  pytest-cache:
  ruff-cache:
